// dispositivo-dashboard.model.ts
export interface DispositivoDashboard {
  id: string;
  nome: string;
  localizacao: string;
  status: string;
  ultima_atualizacao: string;
  rachaduras_detectadas: string;
}
